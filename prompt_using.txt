生成一份python代码，代码应用场景：在同一台机器、同一个操作系统安装、同一个帐号下，不同的人使用不同的git帐号和不同的<sshkey>进行包括但不限于git push、git pull的remote操作。具体需求：1、当git在进行包括但不限于git push、git pull的remote操作的时候，如果remote url为ssh协议（ssh://）或者git协议（git@<host_name>）的时候，该脚本才会进行实际操作，其余情况该脚本进行空操作；2、url的格式为“git@<host_name>:<user_or_group_name>/<repo_name>，或者”ssh://git@<host_name>/<user_or_group_name>/<repo_name>“，根据url格式，获取<host_name>和<user_or_group_name>字段；3、根据<user_or_group_name>和<host_name>这2个字段信息，唯一确定使用哪个<sshKey>；4、(<host_name>, <user_or_group_name>)与<sshKey>的对应关系由人工配置文件给出，配置文件格式为yaml，<sshKey>如果只给文件名称，则在~/.ssh/ （或者windows上相似的路径）文件夹下搜索，没有则报错；如果给完整路径（绝对路径或者相对路径），则直接搜索给出的该路径，没有则报错；5、该脚本应该是对整个操作系统的环境全局生效，可以同时适配windows和linux系统，使用git的配置功能而不要使用操作系统的环境变量。根据以上需求，生成python代码，并且生成建议的配置文件，生成的建议配置文件为示范，格式正确、信息完整即可。